description "reviewqueue"

start on (filesystem)
stop on runlevel [016]

respawn
setuid ubuntu
setgid ubuntu
chdir /opt/reviewqueue

pre-start exec /opt/reviewqueue/.venv/bin/initialize_db /etc/reviewqueue.ini
exec /opt/reviewqueue/.venv/bin/pserve /etc/reviewqueue.ini
